# Signaly — Apache リバースプロキシ設定
# signaly.gucchii.com など専用サブドメインの VirtualHost (*:443) 内に追記する。
# ルート (/) を uvicorn (127.0.0.1:8002) にプロキシする。

    ProxyPreserveHost On
    ProxyPass        / http://127.0.0.1:8002/ flushpackets=on
    ProxyPassReverse / http://127.0.0.1:8002/

    # SSE 用タイムアウト調整（デフォルト 300 秒から延長）
    <Location /api/stream/>
        ProxyPass        http://127.0.0.1:8002/api/stream/ flushpackets=on timeout=3600
        ProxyPassReverse http://127.0.0.1:8002/api/stream/
    </Location>
